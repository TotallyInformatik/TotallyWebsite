/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["totallylinks.appspot.com", "firebasestorage.googleapis.com", ""]
  }
}

module.exports = nextConfig
